import type { SceneObject } from './types';

/** Scene-objects for the EXODO era. Populated by the exodo era agent
 *  in Phase 1 (see docs/superpowers/STYLE_GUIDE_ANIMACIONES.md). */
const SCENE_OBJECTS: Record<string, SceneObject[]> = {};

export default SCENE_OBJECTS;
