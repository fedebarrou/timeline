import type { DialogCue } from '../dialogTypes';

/** Char-to-char dialogs for the EXODO era. Populated by the exodo
 *  era agent in Phase 1. */
const DIALOGS: Record<string, DialogCue[]> = {};

export default DIALOGS;
